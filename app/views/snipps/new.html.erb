<%= render partial: 'steps', locals: {step_one: true, step_label: "Create"} %>
<h3>New snipp</h3>

<%= render 'form' %>

<div class="medium default btn">
  <%= link_to snipps_path do %>
    <i class="icon-left-bold"></i> Back
  <% end %>
</div>
