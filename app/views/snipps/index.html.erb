<% content_for :facebook do %>
  <meta property="og:image" content="<%= asset_url('img_silence_demo.jpg') %>" />
  <meta property="og:title" content="Gumsnipp free code snippets for Gumby" />
  <meta property="og:url" content="<%= root_url %>" />
  <meta property="og:description" content="Design elements, playground and code snippets for Gumby HTML/CSS/JS framework" />
<% end %>

<% @snipps.each_slice(3) do |snipps| %>
  <div class="row">
    <% snipps.each do |snipp| %>
      <div class="four columns thumbnail">
        <h5>
          <%= link_to truncate(snipp.title, length: 28), snipp %>
          <li class="primary label ttip pull_right" data-tooltip="Gumby version">
            <%= link_to snipp.framework_version, snipps_path(framework_version: snipp.framework_version) %>
          </li>
          <span class="ttip pull_right" data-tooltip="Viewed">
            <%= number_to_human(snipp.visits_count) %><i class="icon-eye"></i>
          </span>
        </h5>
        <img src="http://api.webthumbnail.org?width=500&height=400&screen=1024&url=<%= iframe_url(snipp.id,snipp.snipp_version) %>" alt="Snipp Screenshot">
        <div class="field">
          <div class="medium primary btn" style="width:100%"><%= link_to "View", snipp %></div>
        </div>
      </div>
    <% end %>
  </div>
  <br>
<% end %>

<div class="blog-pager" id="blog-pager">
  <%= will_paginate @snipps, :inner_window => 2, :outer_window => 1 %>
</div>
